@model IEnumerable<SteamBoat.Models.FeederUrl>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Url)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.isJSON)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mission)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Url)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.isJSON)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mission.MissionId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FeederId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FeederId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FeederId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
